import { createAction } from "redux-actions";

export const ADD_TASK = createAction("ADD_TASK");
export const DONE_TASK = createAction("EDIT_TASK");
export const DELETE_TASK = createAction("DELETE_TASK");
